-- seeds/test_data.sql
-- Sample rows for development/testing only. Never run in production.

-- Test lead (hot lead)
-- INSERT INTO leads (...) VALUES (...);

-- Test conversation
-- INSERT INTO conversations (...) VALUES (...);

-- Test inbound message (hot lead)
-- INSERT INTO messages (...) VALUES (...);
