-- 05_message_topics.sql
-- Tags and topics extracted from messages by AI classifier

-- Placeholder: to be implemented in database build step

CREATE TABLE message_topics (
  -- id, message_id, lead_id, topic, intent, created_at
);
