-- 03_conversations.sql
-- Tracks conversation sessions per lead per channel

-- Placeholder: to be implemented in database build step

CREATE TABLE conversations (
  -- id, lead_id, channel, platform_conversation_id,
  -- status (open/closed), created_at, updated_at
);
