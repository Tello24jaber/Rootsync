-- 09_weekly_insights.sql
-- Stores generated weekly insight reports

-- Placeholder: to be implemented in weekly insights build step

CREATE TABLE weekly_insights (
  -- id, week_start, week_end,
  -- total_messages, new_leads, hot_leads, cold_leads,
  -- not_interested, needs_human,
  -- top_questions (jsonb), top_health_topics (jsonb),
  -- most_common_objections (jsonb), best_channel,
  -- suggested_content (text), suggested_improvements (text),
  -- report_text (text), created_at
);
