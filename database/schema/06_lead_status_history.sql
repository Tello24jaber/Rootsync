-- 06_lead_status_history.sql
-- Audit trail of every lead status change

-- Placeholder: to be implemented in database build step

CREATE TABLE lead_status_history (
  -- id, lead_id, previous_status, new_status,
  -- changed_by (ai/human/system), reason, created_at
);
